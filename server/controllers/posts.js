export const getPosts= (req,res) => {
    res.send("this works"); // call back function is executed when someone visits localhost3000/
}

export const createPost= (req,res) => {
    res.send("post created"); // call back function is executed when someone visits localhost3000/
}

